# Autogenerated from C:/Users/Yasho/LoopHole/Polygeist/tools/cgeist/Test/lit.site.cfg.in
# Do not edit!

# Allow generated file to be relocatable.
import os
import platform
def path(p):
    if not p: return ''
    # Follows lit.util.abs_path_preserve_drive, which cannot be imported here.
    if platform.system() == 'Windows':
        return os.path.abspath(os.path.join(os.path.dirname(__file__), p))
    else:
        return os.path.realpath(os.path.join(os.path.dirname(__file__), p))


config.llvm_src_root = path(r"C:/Users/Yasho/LoopHole/Polygeist/llvm-project/llvm")
config.llvm_obj_root = path(r"C:/Users/Yasho/LoopHole/build")
config.llvm_tools_dir = path(r"C:/Users/Yasho/LoopHole/build/./bin")
config.polygeist_tools_dir = path(r"C:/Users/Yasho/LoopHole/build/bin")
config.lit_tools_dir = ""
config.mlir_clang_obj_root = "C:/Users/Yasho/LoopHole/build/tools/polygeist/tools/cgeist/Test"
config.target_triple = ""
config.llvm_obj_root = path(r"C:/Users/Yasho/LoopHole/build")
config.polygeist_enable_cuda = "0"
config.cudart_static_path = ""
config.polygeist_enable_rocm = "0"
config.polymer_enabled = "0"
config.polymer_pluto_enabled = "0"

# Support substitution of the tools and build_mode with user parameters.
# This is used when we can't determine the tool dir at configuration time.
try:
    config.llvm_tools_dir = config.llvm_tools_dir % lit_config.params
except KeyError as e:
    key, = e.args
    lit_config.fatal("unable to find %r parameter, use '--param=%s=VALUE'" % (key,key))

import lit.llvm
lit.llvm.initialize(lit_config, config)

# Let the main config do the real work.
lit_config.load_config(config, "C:/Users/Yasho/LoopHole/Polygeist/tools/cgeist/Test/lit.cfg")
